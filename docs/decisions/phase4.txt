**DATA LAYER**
TechnicianWorkingHours
    technician
    timezone
    schedule_blob (JSONField)


2. AvailabilityRecord:
AvailabilityService owns availability generation.
Availability is derived, meaning that Availability is not a domain entity.
Availability should usually be computed from:
WorkingHours
BlackoutDates
Bookings
BookingDays
and returned by: AvailabilityService


3. Technician on Booking:
I approved:
One technician
One booking at a time
Database-level overlap prevention
since model design demands:
database MUST enforce overlap
then Booking must contain technician FK
This is required for:
UniqueConstraint
ExclusionConstraint
Overlap prevention
Scheduling queries
Calendar rendering
Availability generation
I approve:
Booking.technician
with explicit documentation:
Booking.technician must mirror
Request.assigned_technician.

so that I will end up enforcing everything in services and lose database protection.




 Phase 4A Data Layer Audit

  Model Inventory

  PASS

   * Booking exists (5.1.1).
   * WorkingHours exists (5.1.2).
   * BlackoutDate exists (5.1.3).
   * RescheduleRecord exists (5.1.5).
   * NoShowRecord exists (5.1.6).
   * BookingDay exists (Required by instruction and 5.1.1 "MULTI-DAY" rule).
   * AvailabilityRecord correctly absent (Excluded by implementation instruction).

  Field Verification

  FAIL

   * Model: Booking
   * Field: correlation_id
   * Issue: Missing field.
   * Document Reference: booking-audit-spec.md 5.3 requires every audit entry/transition to include a correlation_id. While not in 5.1.1, it is required for forensic linkage defined
     in the audit spec.
   * Model: RescheduleRecord / NoShowRecord
   * Field: correlation_id
   * Issue: Missing field.
   * Document Reference: booking-audit-spec.md 5.3. These models are defined as "Audit entities" (5.1.5, 5.1.6) and must store the correlation_id to trace transactional lineage.

  Enum Verification

  PASS

   * Booking.Status: unscheduled, scheduled, in_progress, completed, cancelled, no_show (Matches booking-state-machine.md 5.1).
   * NoShowRecord.AbsentParty: customer, technician (Matches booking-model-design.md 5.1.6).

  Constraint Verification

  FAIL

   * Constraint: Double-Booking Overlap Prevention
   * Issue: Missing ExclusionConstraint or equivalent implementation in Meta.constraints.
   * Document Reference: booking-model-design.md 5.3: "The database MUST enforce a constraint... ensuring no two Booking records... have overlapping windows."
   * Constraint: Blackout Date Overlap Constraint
   * Issue: Missing constraint to prevent blackouts from overlapping existing bookings.
   * Document Reference: booking-model-design.md 5.3: "The database must reject any BlackoutDate record that overlaps with an existing scheduled or in_progress Booking."

  Index Verification

  PASS

   * Booking: Index on status, start_time, end_time exists.
   * RescheduleRecord: Ordering index on created_at exists.

  State Machine Alignment

  PASS

   * All 6 states from booking-state-machine.md 5.1 are represented in Booking.Status.

  Event Alignment

  PASS

   * Required IDs (request_id, booking_id, actor_id) exist across the model layer.
   * Note: correlation_id alignment is reported under Field Verification.

  Audit Alignment

  FAIL

   * RescheduleRecord and NoShowRecord lack correlation_id for "event tracing" and "correlation tracking" as mandated by booking-audit-spec.md 5.3.

  Over-Implementation Findings

   * None. Implementation is strictly limited to documented requirements.

  Under-Implementation Findings

   1. Missing correlation_id on Booking, RescheduleRecord, and NoShowRecord.
   2. Missing ExclusionConstraint on Booking for technician overlap prevention.
   3. Missing Overlap Constraint on BlackoutDate relative to Booking.
   4. Missing started_at on Booking model to support booking.in_progress audit requirements (booking-audit-spec.md 5.2.1) and event payload (booking-event-contracts.md 5.2.5).

  Risk Assessment

  MEDIUM

  The absence of database-level exclusion constraints poses a risk of double-bookings during high-concurrency scheduling operations. The lack of correlation_id prevents full
  forensic traceability required by the Audit Spec.

  Production Readiness

  NOT READY

  Blocking Issues

   1. Implementation of ExclusionConstraint for Booking overlap prevention (5.3).
   2. Addition of correlation_id (UUID) to Booking, RescheduleRecord, and NoShowRecord for audit compliance (5.3).
   3. Addition of started_at (DateTimeField) to Booking to support the in_progress transition (5.2.5).
   4. Implementation of Blackout overlap check at the database/model level (5.3).

